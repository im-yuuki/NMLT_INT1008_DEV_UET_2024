#include <bits/stdc++.h>
using namespace std;

// [File]
void housesOfHogwarts(const char* path) {
    ifstream file(path);
    string line;
    getline(file, line);
    while(getline(file, line)) cout << line << endl;
}